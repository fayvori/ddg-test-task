docker_users:
  - debian

grafana_admin_password: "secure_pass"

additional_scrape_targets:
  - job_name: "node_exporter"
    scrape_interval: "5s"
    static_configs:
      - targets: "{{ _prometheus_scrape_targets }}"
        labels:
          service: "iperf3"
